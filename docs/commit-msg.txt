feat: lang switcher always visible on mobile nav bar

- index.html: added lang-switcher--nav-mobile between lang-switcher and burger button, always visible in nav bar on mobile
- css/lang.css: added .lang-switcher--nav-mobile styles (hidden on desktop, flex on mobile <=768px); updated media query to exclude new class from desktop-hide rule; added RTL override for HE
- JS unchanged: querySelectorAll('.lang-btn') already captures all instances
